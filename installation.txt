# assume node.js is already installed
# in terminal run this to install Angular CLI

npm install -g @angular/cli
ng new TicTacToe

npm i @angular/service-worker

# angular inside project
ng add @nebular/theme

ng add @angular/pwa --project 
ng build --prod
# connect to firebase
npm install -g firebase-tools
#ng deploy


Visual Studio Code Extensions
1. Angular Language Service         # syntax highlights for HTML Templates
2. Angular Console (Nx Console)     # use CLI via Graphical User Interface